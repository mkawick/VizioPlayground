using UnityEngine;

public class JunkTest_DeleteMe : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        if(this.didAwake)
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
